import CHORE_DATA from './chore-data';

const INITIAL_STATE = {
  collections: CHORE_DATA
};

const choreReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
      default: 
        return state
  }
};

export default choreReducer;